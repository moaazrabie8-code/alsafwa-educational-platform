# تقرير تدقيق الأمن والموثوقية والأداء — النسخة المستقرة

**النطاق:** تطبيق React/Vite وExpress/tRPC وDrizzle/MySQL، وفق الشفرة المعتمدة في نقطة الحفظ `4d038267` وفحص النطاق المنشور في 23 أغسطس 2026. كانت النسخة الأولى من التقرير خط أساس قبل التعديل؛ يوضح قسم **نتيجة المعالجة والتحقق** أدناه التغييرات التي طُبقت بعد ذلك على المصدر.

> **الخلاصة التنفيذية:** لم يظهر استغلال حرج مباشر في مسارات التعليم المفحوصة، وتحتوي إجراءات الطالب والمعلّم على فحوص ملكية جيدة في الغالب. لكن توجد مخاطر عالية في إدارة الجلسات وCSRF وتعطيل حساب مدير وسلسلة الاعتمادات. كما أن بوابة Manus الخارجية تمنع دخول الحسابات المدعوة قبل وصولها للتطبيق، وهي مشكلة وظيفية/تشغيلية مستقلة عن صلاحيات التطبيق الداخلية.

## منهج التحقق وحدوده

شمل التدقيق تهيئة Express، OAuth وJWT والكوكي، تهيئة tRPC، راوترات المدير/المعلّم/الطالب/الملف، مخطط Drizzle، وكيل التخزين، واجهة العميل، مخرجات البناء، تدقيق اعتماديات الإنتاج، والرؤوس الظاهرة على النطاق المنشور. **يتطلب تحققًا يدويًا**: صلاحيات مستخدم قاعدة البيانات، قواعد بوابة الاستضافة، تدفق OAuth ببيانات حساب حقيقية، وإتاحة الموقع للزوار الخارجيين.

## ملخص الأولويات

| الدرجة | العدد | الحالة |
|---|---:|---|
| حرجة | 0 | لم تُثبت في الشفرة المفحوصة. |
| عالية | 0 مفتوحة | عولجت H-01 إلى H-04 في المصدر والتحقق الآلي؛ يلزم اختبار OAuth الإنتاجي فقط. |
| متوسطة | 4 مفتوحة | بقيت قيود العلاقات وقصور التخزين العام والصورة اليتيمة وبوابة الدخول الخارجية. |
| منخفضة/معلومة | 5 | تحسينات أداء ووصول وSEO وتحقيقات تشغيلية لاحقة. |

## النتائج عالية الأولوية

| المعرف | المشكلة ولماذا تهم | الملفات/الدالة | السبب الجذري | الإصلاح الآمن | خطر الانحدار والتحقق |
|---|---|---|---|---|---|
| H-01 | يمكن لحساب مدير **معطّل** الاستمرار في استخدام إجراءات المدير إذا بقيت لديه جلسة صالحة؛ `adminProcedure` يفحص الدور فقط ولا يمر عبر `requireUser` الذي يفحص `isActive`. | `server/_core/trpc.ts`، `adminProcedure`، وكل `admin.*` | فصل مساري `protectedProcedure` و`adminProcedure`. | بناء `adminProcedure` فوق نفس فحص المستخدم النشط، ثم اختبار مدير نشط/معطّل. | منخفض؛ تحقق من أن المدير النشط يبقى قادرًا على الإدارة وأن المعطّل يحصل على `FORBIDDEN`. |
| H-02 | جلسة JWT مدتها سنة كاملة بلا `sessionVersion` أو قائمة إبطال أو تدوير عند تعطيل الحساب/تغيير الدور. كما يسمح الخادم بـBearer بديل وتحتفظ الواجهة به في `sessionStorage`. | `server/_core/sdk.ts`، `server/_core/oauth.ts`، `client/src/main.tsx` | اعتماد جلسة طويلة الأجل وبديل معاينة مفعّل في الإنتاج. | تقصير العمر إلى 8–12 ساعة، تضمين إصدار جلسة خادمي، زيادته عند التعطيل/تغيير الدور، وقصر Bearer على التطوير فقط. | متوسط؛ تحقق من تسجيل الدخول وتسجيل الخروج وتعطيل الحساب وتغيير الدور. |
| H-03 | كوكي الجلسة `SameSite=None` وتُرسل في طلبات عابرة للموقع، ولا توجد حماية CSRF أو تحقق `Origin` لمسارات tRPC المغيرة للحالة. | `server/_core/cookies.ts`، `server/_core/index.ts`، طفرات `admin/teacher/student/profile` | مصادقة مبنية على الكوكي بلا دفاع إضافي للطلبات العابرة للموقع. | تحويل الكوكي إلى `SameSite=Lax` إن لم يتأثر OAuth، وإضافة تحقق Origin/Host متحفظ لمسارات التعديل؛ أو CSRF token عند وجود تدفق يحتاج `None`. | متوسط؛ تحقق من callback OAuth ورفع الصورة وطفرات tRPC من نفس النطاق. |
| H-04 | تدقيق اعتماديات الإنتاج أعاد **3 تحذيرات عالية و30 متوسطة و7 منخفضة**. العالية عبر Express 4 (`path-to-regexp`) وRecharts (`lodash`) وStreamdown/Mermaid (`lodash-es`). | `package.json`، `pnpm-lock.yaml` | النسخة المرفقة تعيد Express 4.21.2 وRecharts 2.15.4 وStreamdown 1.4.0. | ترقية متحكم بها إلى نسخ مصححة، مع اختبار مسارات Express والرسوم قبل النشر. لا تستخدم ترقية جماعية عمياء. | متوسط؛ Express 5 يغير صياغة wildcard، لذا تُراجع `storageProxy.ts` و`vite.ts` قبل الترقية. |

## نتيجة المعالجة والتحقق

عولجت البنود ذات الأولوية على المصدر الحالي دون إضافة نظام دخول جديد أو تغيير تدفقات التعليم والواجبات والاختبارات. أصبح `adminProcedure` مبنيًا فوق تحقق المستخدم النشط، وبذلك يرفض المدير المعطّل حتى لو حمل جلسة سابقة. أصبحت الجلسة مدتها 12 ساعة، وتحمل `sessionVersion` المطابق لقاعدة البيانات، ويزداد الإصدار عند تعطيل الحساب أو تغيير دوره، كما اقتصر Bearer البديل على بيئة التطوير.

| المعرف | التغيير المنفذ | نتيجة التحقق | القيد المتبقي |
|---|---|---|---|
| H-01 | ربط إجراءات المدير بتحقق المستخدم النشط، مع اختبار مدير معطّل. | اختبارات الصلاحيات نجحت. | لا يوجد قيد شفرة معروف. |
| H-02 | جلسة JWT لمدة 12 ساعة مع `sessionVersion` وإبطال عند تعطيل الحساب أو تغيير الدور؛ إيقاف Bearer في الإنتاج. | نجح اختبار إصدار الجلسة واختبارات الإدارة، وتأكد بناء الإنتاج من غياب `Authorization: Bearer` التطويري من حزمة العميل. | يلزم اختبار تسجيل الدخول/الخروج وتعطيل حساب فعليًا على النطاق المنشور. |
| H-03 | `SameSite=Lax`، ورفض طفرات tRPC بلا Origin مطابق، وحد طلبات متحفظ لـOAuth وtRPC. | فُحص محليًا: طلب تعديل بلا Origin أعاد 403 وطلب من نفس المصدر أعاد 200. | الحد داخل ذاكرة العملية فقط؛ يلزم WAF أو بوابة موزعة عند التوسع الأفقي، كما يلزم اختبار OAuth المنشور. |
| H-04 | ترقية Express إلى 5.2.1 وRecharts إلى 3.10.1 وStreamdown إلى 2.5.0، وتحديث wildcards ومسارات أنواع الرسم البياني. | `pnpm audit --prod` أعاد “No known vulnerabilities found”، ونجحت الأنواع والاختبارات والبناء. | راقب رسوم لوحة البيانات خلال اختبار المستخدم الحقيقي. |
| M-02 | تحقق بصمة JPEG/PNG/WebP قبل تخزين الصورة الشخصية. | اختبار PNG صحيح ورفض بيانات متنكرة ناجحان. | الفحص لا يستبدل معالجة صور كاملة أو فحص الأبعاد مستقبلًا. |
| M-05 | إضافة `X-Content-Type-Options` وHSTS عند HTTPS، مع الإبقاء على CSP المتوافقة مع المعاينة. | أعاد طلب محلي للرئيسية CSP و`nosniff` وReferrer-Policy وPermissions-Policy؛ كما مر مسار تخزين ذي مفتاح عبر الوكيل وأعاد 307. كما أعاد النطاق المنشور قبل الدخول 302 مع HSTS و`nosniff` من الحافة. | يلزم فحص رؤوس صفحة التطبيق بعد تسجيل الدخول؛ لا يظهر HSTS محليًا لأن الاتصال غير HTTPS. |

> لا تُنفّذ الهجرة `drizzle/0007_normal_wither.sql` على قاعدة البيانات الحالية: عمود `sessionVersion` موجود فيها بالفعل. أضيفت الهجرة فقط لمزامنة المصدر؛ تطبيقها مجددًا قد يفشل بسبب وجود العمود.

## تحقق الانحدار بعد المعالجة

نفذت فحوص TypeScript وبناء الإنتاج واختبارات Vitest بنجاح. النتيجة الحالية هي **8 ملفات اختبار و41 اختبارًا ناجحًا**، كما أعاد تدقيق اعتماديات الإنتاج نتيجة «لا توجد ثغرات معروفة». أدى تثبيت `NODE_ENV=production` في أمر البناء أيضًا إلى إنتاج الحزمة الفعلية من دون شيفرة التطوير؛ أصبحت الحزمة الرئيسية تقارب **447KB مصغرة و136KB مضغوطة**. ما زال التحميل والأداء على شبكة هاتف حقيقية بحاجة إلى قياس يدوي.

## النتائج المتوسطة

| المعرف | المشكلة ولماذا تهم | الملفات/الدالة | الإصلاح الآمن | التحقق |
|---|---|---|---|---|
| M-01 | لا يوجد حد طلبات صريح على OAuth أو API، ما يسهل الإساءة والطلبات المتكررة. | `server/_core/index.ts`، `oauth.ts`، `/api/trpc` | حد دفاعي لكل IP لمسار OAuth والطفرات؛ ويلزم حد حافة/بوابة عند التوسع الأفقي. | اختبارات 429 وحدود إعادة المحاولة. |
| M-02 | التحقق من الصورة يعتمد على Data URL وMIME والحجم، ولا يثبت بصمة JPEG/PNG/WebP الثنائية أو الأبعاد. | `server/domain/profileRules.ts`، `profile.uploadAvatar` | فك ترميز محدود الحجم والتحقق من magic bytes قبل `storagePut`. | قبول صورة صحيحة ورفض محتوى متنكر. |
| M-03 | لا توجد مفاتيح أجنبية في المخطط؛ تكامل العلاقات يعتمد على إجراءات التطبيق فقط. | `drizzle/schema.ts` | إضافة FKs تدريجيًا بعد فحص بيانات يتيمة، أو توثيق سبب عدم دعمها في TiDB الحالي وإجراء checks دورية. | استعلام بيانات يتيمة قبل كل ترحيل. |
| M-04 | وكيل التخزين يعيد رابطًا موقّعًا لأي مفتاح معروف، بلا تحقق ملكية. الصورة الشخصية متوقعة أن تكون عامة حاليًا، لكن النمط غير صالح لمرفقات خاصة مستقبلًا. | `server/_core/storageProxy.ts` | الإبقاء على الصور العامة بوضوح، وإضافة metadata/تفويض للمحتوى الخاص مستقبلًا. | تحقق يدوي للروابط العامة والحالات الخاصة. |
| M-05 | ترويسات التطبيق لا تضبط `X-Content-Type-Options` أو HSTS أو `X-Frame-Options` صراحة؛ الحافة تضبط HSTS و`nosniff` حاليًا، لكن لا ينبغي افتراض استمرار ذلك. | `server/_core/index.ts`، فحص النطاق المنشور | إضافة رؤوس محافظة للتطبيق، وتجربة CSP قبل فرضها؛ اختيار سياسة إطارات صريحة وفق حاجة المعاينة. | فحص رؤوس الإنتاج وOAuth والمعاينة. |
| M-06 | عملية رفع الصورة تكتب للتخزين ثم لقاعدة البيانات؛ إذا فشلت الثانية قد تبقى صورة يتيمة. | `profile.uploadAvatar` | توثيق القيد الآن؛ عند توفر حذف تخزين مدعوم، تنفيذ تعويض أو مهمة تنظيف آمنة. | محاكاة فشل تحديث DB في اختبار. |
| M-07 | تدفق دعوات الحساب يعتمد على OAuth الخارجي؛ بوابة Manus ترفض المستخدم الخارجي قبل وصوله لمنطق ربط البريد، فتظهر «أذونات غير كافية». | الاستضافة/بوابة Manus، `server/db.ts` | تم جعل الموقع Public، ثم عولجت جلسات الاسم الفارغ وأضيف زر دخول صريح لمسار `/portal`. | أكد المستخدم نجاح دخول الحسابات غير الإدارية بعد النشر. |

## الجودة والأداء والوصول وSEO

| المجال | الملاحظة | الأثر | المعالجة الآمنة المقترحة |
|---|---|---|---|
| الأداء | الحزمة الرئيسية الآن ~447KB مصغرة (~136KB gzip) في بناء الإنتاج. | تحسن ملموس مقارنة بخط الأساس، لكن القياس على شبكة هاتف حقيقية مؤجل. | تحليل bundle وتقسيم المكتبات الثقيلة مثل الرسوم/Markdown عند الاستعمال الفعلي. |
| الوصول | أضيف `:focus-visible` متباين للروابط والأزرار والحقول وعناصر التحكم. | أصبح موضع لوحة المفاتيح مرئيًا بوضوح. | اختبار Tab/Shift+Tab وقارئ الشاشة يدويًا على التدفقات المحمية. |
| SEO | أضيفت description وcanonical وOpen Graph و`robots.txt` بعد إتاحة الموقع، وتأكدت هذه العناصر على النطاق المنشور. | تحسن أساس المشاركة والفهرسة للواجهة العامة. | لا يُضاف sitemap قبل استقرار الصفحات العامة القابلة للفهرسة. |
| أخطاء العميل | يسجل العميل أخطاء API في console ويعرض الرسالة أحيانًا عبر toast. | احتمال ظهور رسالة تقنية إذا تغيّرت رسائل الخادم. | خريطة رسائل عامة للمستخدم والاحتفاظ بالتفاصيل في logging خادمي. |
| XSS | لم يظهر `dangerouslySetInnerHTML` أو `innerHTML` أو `eval` في مكونات TSX المفحوصة. React escaping يغطي العرض النصي المعتاد. | نتيجة إيجابية، وليست ضمانًا للـMarkdown أو الحزم الخارجية. | الحفاظ على عدم السماح بـHTML من المستخدم واختبار أي ميزة Markdown مستقبلية. |

## نتائج إيجابية موثقة

تستخدم غالبية إجراءات الطالب والمعلّم فحص دور خادمي وربطًا صريحًا بالمجموعة أو المحاولة. يوجد قيد فريد للبريد ولعضوية الطالب بالمجموعة ولرقم محاولة الاختبار. كما تُستخدم معاملات في مسارات الحضور وإنشاء الاختبارات وتسليمها ومراجعة المقالات في النسخة المفحوصة. تُستخدم Zod للحدود والأنواع على مدخلات API الأساسية، ولا توجد دلائل على SQL خام من إدخال المستخدم في المسارات المفحوصة.

## ترتيب الإصلاح المقترح قبل أي تطوير جديد

1. إصلاح H-01 ثم H-02 وH-03، مع اختبارات مصادقة وصلاحيات.
2. تحديث الاعتمادات ذات الأولوية H-04 بتغيير متحكم به واستعادة التوافق.
3. إضافة حد الطلبات وفحص بصمة الصورة، ثم الرؤوس المحافظة.
4. اتخاذ قرار إتاحة الموقع Public وحل تدفق الدخول الخارجي؛ هذا شرط لتجربة الطلاب والمعلّمين فعليًا.
5. معالجة SEO وملف bundle والوصول بعد استقرار الدخول.

## عناصر «تتطلب تحققًا يدويًا»

لا يمكن للشفرة وحدها تأكيد امتيازات مستخدم قاعدة البيانات أو النسخ الاحتياطي والاستعادة أو سياسات WAF/Rate Limit في الحافة أو عدم تعرّض sourcemaps بعد النشر. أكد المستخدم نجاح تدفق OAuth ورفع صورة الملف الشخصي من حساب فعلي على النطاق المنشور. يبقى تفويض ملفات مستقبلية خاصة وتجربة حدود الطلبات تحت حمل حقيقي من التحققات التشغيلية اللاحقة.
