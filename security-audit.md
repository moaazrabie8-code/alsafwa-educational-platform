# تدقيق الأمن والموثوقية والأداء — منصة الصفوة التعليمية

**تاريخ المراجعة:** 23 أغسطس 2026  
**النطاق:** خادم Express وtRPC، المصادقة والجلسات، صلاحيات الأدوار، قاعدة البيانات، رفع صورة الملف الشخصي، واجهة React، الاعتمادات، مسار النشر، والأداء الأولي.  
**حالة التقرير:** تم تنفيذ الإصلاحات الآمنة المبينة أدناه، بينما تبقى العناصر الموسومة **يتطلب تحققًا يدويًا** أو **مؤجلًا بقرار هندسي** ضمن خطة المتابعة.

> لا يثبت هذا التقرير عدم وجود كل الثغرات. هو مراجعة بنيوية مدعومة بفحص شفرة واختبارات آلية وفحص حزم وقت المراجعة، ولا يشمل اختبار اختراق خارجي أو مراجعة إعدادات البوابة والاستضافة التي لا تظهر في المستودع.

## الملخص التنفيذي

لم تُظهر المراجعة استخدامًا مباشرًا لـ `dangerouslySetInnerHTML` أو `innerHTML` أو `eval` أو `new Function` في شفرة العميل، وتُطبّق إجراءات tRPC المحمية تحققًا من الدور والحالة النشطة. كما أن مسارات الطالب والمعلّم الرئيسية تتحقق من ملكية المجموعة أو المحاولة قبل القراءة أو التعديل. إلا أن المراجعة كشفت نقاطًا كانت تستحق معالجة فورية: إمكان تمرير معرف درس/مادة لا يتوافق مع المجموعة عند إنشاء واجب أو اختبار، وغياب تفرد البريد الإلكتروني رغم اعتماده لربط الدعوات، ورؤوس حماية أساسية غير مفعلة صراحةً، وحد طلب عام أكبر من الحاجة، واعتمادات إنتاجية متأخرة أمنيًا.

تمت معالجة هذه النقاط الآن مع اختبارات انحدار. انخفضت نتيجة تدقيق اعتماديات الإنتاج من **1 حرج، و21 عالي، و49 متوسط، و10 منخفض** إلى **0 حرج، و3 عالٍ، و30 متوسط، و7 منخفض**. العناصر العالية المتبقية تقع في تبعيات عميقة أو ترقيات تحتاج اختبار توافق مستقل، ولا ينبغي تجاوزها بتحديث قسري.

| الحالة | العدد/النتيجة | القرار |
|---|---:|---|
| إصلاحات مكتملة | 5 محاور | مطبقة ومغطاة باختبارات وبناء ناجح. |
| تحذيرات إنتاجية حرجة بعد التحديث | 0 | لا توجد نتائج حرجة في تدقيق الحزم اللاحق. |
| تحذيرات إنتاجية عالية بعد التحديث | 3 | تحتاج ترقية مكونات عليا أو تحقق توافق، موضحة أدناه. |
| اختبارات المشروع | 35 ناجحة | شملت حواجز الصلاحيات الجديدة. |
| بناء الإنتاج | ناجح | بقي تحذير حجم حزمة العميل الرئيسية فقط. |

## الإصلاحات التي نُفذت

| الأولوية السابقة | المشكلة والجذر | الملفات المتأثرة | الإصلاح الآمن المنفذ | مخاطر الانحدار | التحقق المنفذ |
|---|---|---|---|---|---|
| عالية | كان المعلّم يستطيع اختيار `lessonId` من مجموعة أخرى عند إنشاء واجب، لأن الخادم يتحقق من ملكية `groupId` فقط. | `server/routers/teacher.ts` — `assignments.create` | التحقق من أن الدرس موجود وينتمي إلى المجموعة نفسها قبل الإدراج. | قد تتلقى الواجهة رسالة رفض إذا أرسلت معرف درس قديمًا بعد تبديل المجموعة، وهذا سلوك صحيح. | اختبار يمنع الإدراج عند عدم تطابق الدرس؛ ناجح. |
| عالية | كان إنشاء الاختبار يثق في `subjectId` القادم من العميل ولم يربطه بمادة المجموعة، وكذلك لم يتحقق من درس الاختبار الاختياري. | `server/routers/teacher.ts` — `exams.create` | ربط مادة الاختبار بـ `subjectId` الفعلي للمجموعة والتحقق من علاقة الدرس بالمجموعة. | لا يوجد أثر على التدفقات السليمة؛ الطلبات المتلاعب بها سترفض. | اختبار انحدار للموضوع غير المطابق؛ ناجح. |
| عالية | البريد الإلكتروني مستخدم لربط الحسابات المسبقة عند أول OAuth، لكنه لم يكن مقيدًا بالتفرد ولم يتحقق `people.update` من التعارض. | `drizzle/schema.ts`، `drizzle/0006_late_ulik.sql`، `server/routers/admin.ts` | فحص البيانات الحالية أولًا، ثم قيد فريد `users_email_unique` في قاعدة البيانات، مع تطبيع البريد إلى حروف صغيرة وفحص تضارب صريح في الإنشاء والتعديل. | ترفض الآن محاولة تكرار بريد بأحرف مختلفة؛ هذا مقصود لحماية ربط الهوية. | استعلام التكرار قبل الترحيل لم يرجع صفوفًا، والترحيل طُبق، واختبار التعارض ناجح. |
| متوسطة | الاستجابة كانت تكشف ترويسة إطار العمل، ولا تضبط حماية الإطارات/الإحالة/ميزات المتصفح صراحة. | `server/_core/index.ts` | تعطيل `x-powered-by` وإضافة CSP متحفظة (`base-uri` و`object-src`) وقائمة `frame-ancestors` تسمح فقط بالنطاق نفسه ومساحات Manus الموثوقة، مع `Referrer-Policy` و`Permissions-Policy`. | تبقى الإطارات الخارجية غير الموثوقة مرفوضة، بينما تعمل المعاينة داخل Manus. | تحقق HTTP محلي أثبت وجود السياسة والرؤوس وعدم ظهور `x-powered-by`. |
| متوسطة | حد JSON وطلبات URL-encoded كان 50MB عالميًا مع أن الصورة الشخصية الحد الأقصى لها 1.5MB قبل Base64. | `server/_core/index.ts` | تخفيض الحد العام إلى 3MB، وهو مناسب لصورة 1.5MB بعد Base64 مع هامش آمن. | سيُرفض أي عميل يحاول إرسال جسم كبير غير مدعوم؛ لا توجد ميزة معتمدة تحتاج 50MB. | فحص الأنواع والبناء ناجحان؛ اختبار رفع صورة حقيقي ما زال يدويًا. |
| متوسطة | حفظ الحضور وإنشاء الاختبار ومراجعة إجابة مقالية وإنشاء حساب دعوة كانت تتضمن أكثر من كتابة مستقلة. | `server/routers/teacher.ts`، `server/routers/admin.ts` | تغليف تسلسلات الكتابة التابعة في معاملات Drizzle/MySQL؛ إما نجاح كل الخطوات أو التراجع عنها معًا. | لا يتغير السلوك المتوقع؛ قد تحتاج العملية إلى إعادة محاولة بعد خطأ اتصال عابر بدل بقاء حالة جزئية. | فحص الأنواع و38 اختبارًا ناجحًا؛ تغطي اختبارات سير العمل هذه المسارات. |
| متوسطة | طلبان متزامنان لبدء الاختبار قد يتنافسان على رقم محاولة واحد ثم يظهر أحدهما كخطأ قاعدة بيانات. | `server/routers/student.ts` — `exams.start` | الاحتفاظ بالقيد الفريد، ثم التقاط تعارض المفتاح وإرجاع المحاولة النشطة المتزامنة أو رسالة `CONFLICT` عربية. | لا يوجد أثر على الطلب الواحد؛ النقر المزدوج يعود للمحاولة نفسها إن كانت نشطة. | فحص الأنواع والاختبارات ناجحة؛ اختبار حمل متزامن فعلي ما زال ضمن المتابعة. |
| عالية/حرجة في سلسلة التوريد | كان تدقيق اعتماديات الإنتاج يحتوي حزمة حرجة وتحذيرات عالية في AWS SDK وAxios وDrizzle وNanoid وtRPC. | `package.json`، `pnpm-lock.yaml` | تحديث AWS SDK، Axios، Drizzle ORM، Nanoid، وحزم tRPC الثلاث المتطابقة ضمن الإصدارات الرئيسية نفسها. | قد يتغير سلوك اعتمادي دقيق، لذلك نُفذت اختبارات كاملة وبناء بعد التحديث. | انخفض التدقيق إلى 0 حرج و3 عالٍ؛ `pnpm check` و38 اختبارًا والبناء ناجحة. |

## نتائج تتطلب معالجة لاحقة أو قرارًا صريحًا

### 1. الجلسات وCSRF — عالية

> **المشكلة:** مدة JWT الافتراضية في `server/_core/sdk.ts` سنة كاملة، ويقبل الخادم `Authorization: Bearer` كبديل للكوكيز. في المقابل الكوكي مضبوط على `SameSite=None` و`Secure` و`HttpOnly`، ولا توجد طبقة تحقق Origin/CSRF خاصة بالعمليات المغيرة للحالة.

هذا يزيد مدة الاستفادة من جلسة مسروقة، كما أن الرمز المخزن في `sessionStorage` عند استخدام البديل قابل للوصول من JavaScript في حال حدوث XSS. تخفيف `SameSite` أو إلغاء Bearer بلا اختبار قد يعطل OAuth أو معاينات WebView، لذلك لم يُجرَ تغيير قسري.

| الملف/الدالة | المعالجة المقترحة بأمان | مخاطر الانحدار | حالة التحقق |
|---|---|---|---|
| `server/_core/sdk.ts` — `signSession` و`authenticateRequest` | جعل مدة الجلسة إعدادًا قابلًا للضبط وقصرها لمدى أقصر عمليًا، وإضافة إبطال جلسة أو رقم إصدار للجلسة عند تغيير الدور/تعطيل الحساب. | قد يطلب من المستخدمين تسجيل دخول مجددًا، ويحتاج توافقًا مع OAuth الحالي. | **يتطلب تحققًا يدويًا** لتدفق الدخول وWebView. |
| `client/src/main.tsx` و`server/_core/sdk.ts` | حصر Bearer المخزن في `sessionStorage` في التطوير أو مضيفات المعاينة إن لم يكن إنتاجيًا ضروريًا. | قد تتأثر جلسات معاينة أو تطبيق مدمج. | **يتطلب تحققًا يدويًا**. |
| `server/_core/index.ts` أو middleware tRPC | فرض Origin/Referer موثوق للطلبات المغيرة للحالة، أو اعتماد آلية CSRF متوافقة مع OAuth. | يجب اعتماد قائمة نطاقات الإنتاج والمعاينة بدقة. | **يتطلب قرارًا هندسيًا**. |

### 2. معاملات الكتابة وسباقات التزامن — عولجت في المسارات الأساسية

أصبحت عمليات حفظ الحضور وإنشاء الاختبار مع أسئلته، وإعادة احتساب مراجعة الإجابة المقالية، وإنشاء حساب الدعوة مع ملف الدور تعمل ضمن معاملات قاعدة بيانات. لذلك لا يفترض أن تبقى جلسة حضور بلا سجلات، أو اختبار بلا أسئلة، أو حساب دعوة بلا ملف دور إذا فشلت خطوة ضمن التسلسل.

في `student.exams.start` يبقى القيد الفريد على `(examId, studentUserId, attemptNumber)` مصدر الحماية النهائي من التكرار، مع تحويل تعارض المفتاح المتزامن إلى استئناف للمحاولة النشطة أو رسالة تطبيق واضحة بدل خطأ تقني خام. **يتطلب تحققًا يدويًا أو اختبار حمل** إذا ارتفع معدل التزامن فعليًا؛ لا يدّعي هذا التقرير اختبار تنافس متعدد العملاء على الإنتاج.

### 4. التحقق من محتوى الصورة — منخفض إلى متوسط

`server/domain/profileRules.ts` يتحقق من صيغة Data URL وMIME والحجم، لكنه لا يفك الترميز أو يتحقق من البصمة الثنائية لملف JPEG/PNG/WebP. مع كون الصورة العامة هي نوع الرفع الوحيد حاليًا، يظل الأثر محدودًا، لكن يوصى بالتحقق الحقيقي من الملف وحدوده البعدية قبل التخزين عند توسيع الرفع.

كما أن رفع الصورة ينجح في التخزين الخارجي قبل حفظ رابطها في قاعدة البيانات. إذا فشل تحديث قاعدة البيانات بعد الرفع، فقد تبقى صورة يتيمة بمفتاح عشوائي لا يظهر في التطبيق. لا توفر واجهة التخزين الحالية عملية حذف موثقة، ولذلك لم يُضف حذف تخميني قد يسبب فقد بيانات. يوصى بإضافة حذف مدعوم رسميًا أو مهمة تنظيف مفهرسة عند توسيع نظام الملفات، مع الاحتفاظ بهذا القيد ضمن مراجعة التخزين.

### 5. التخزين العام — معلومة تصميمية

`server/_core/storageProxy.ts` يخدم أي كائن معروف مفتاحه دون تفويض إضافي. هذا مناسب حاليًا للصور الشخصية العامة، لكنه **غير مناسب** لأي ملفات واجبات أو مواد خاصة مستقبلًا. عند إضافة مرفقات خاصة يجب حفظ الملكية والوصول في قاعدة البيانات وإصدار روابط مؤقتة فقط بعد تحقق الصلاحية.

### 6. العلاقات المرجعية — متوسطة للموثوقية

المخطط يعتمد غالبًا على تحقق التطبيق والفهارس ولا يعرّف مفاتيح خارجية فعلية لكل العلاقات. هذا لا يمثل خلل صلاحيات بحد ذاته، لكنه يتيح إدخالات يتيمة إذا حدثت كتابة مباشرة أو خلل في تطبيق متعدد الخطوات. يجب تقييم المفاتيح الخارجية تدريجيًا مع مراجعة البيانات الحالية وسياسة الحذف؛ لا ينبغي إضافتها دفعة واحدة على بيانات تشغيلية.

## الاعتمادات المتبقية

بعد التحديث لا توجد نتيجة حرجة، وبقيت ثلاث نتائج عالية موزعة بين `path-to-regexp` في Express 4، و`lodash`/`lodash-es` عبر مكونات العرض. لا توجد ترقية تلقائية آمنة تجمعها جميعًا في التقرير الحالي. يبقى DOMPurify ضمن التحذيرات المتوسطة عبر المسار نفسه ويحتاج مراجعة عند تحديث مكوّن العرض.

| التبعية | المسار | سبب عدم التحديث الآن | الإجراء الموصى به |
|---|---|---|---|
| `path-to-regexp` | `express > path-to-regexp` | المعالجة الآمنة قد تستلزم انتقال Express 5 أو تجاوزًا مدروسًا؛ كلاهما يحتاج اختبار المسارات وOAuth. | تجربة تحديث منفصل في فرع/نقطة استعادة ثم اختبار OAuth وtRPC. |
| `lodash` | `recharts > lodash` | تحديث Recharts من 2 إلى 3 ترقية رئيسية. | مراجعة واجهات الرسوم المستخدمة ثم ترقية مضبوطة أو إزالة المكوّن غير المستعمل. |
| `lodash-es` | `streamdown > mermaid > lodash-es` | المسار داخل مكون عرض؛ تحديث `streamdown` قد يغير واجهات العرض. | مراجعة استخدام `streamdown` وMermaid، ثم تحديث مكون العرض واختبار الصفحات. |

كما أظهر مدير الحزم تحذير توافق بين `@builder.io/vite-plugin-jsx-loc` وVite 7، وتحذير إهمال Recharts 2. هذه ليست نتائج اختراق مؤكدة، لكنها دين تقني يجب جدولة مع اختبار الواجهة. يجب أيضًا نقل إعدادات `pnpm.overrides` و`patchedDependencies` إلى آلية PNPM المدعومة حاليًا، لأن PNPM الحالي يحذّر أنه لا يقرأهما من موضعهما الحالي.

## الأداء والجودة وتجربة المستخدم

البناء ناجح، لكن الحزمة الرئيسية للعميل ما زالت تقارب **679KB مصغرة و198KB مضغوطة**. قُسمت صفحات الأدوار تحميلًا كسولًا بالفعل، لكن ما زالت مكتبات واجهة أو مخطط/أيقونات ضمن الحزمة المشتركة. يوصى بقياس الاستيرادات بواسطة visualizer، ثم نقل المكتبات الثقيلة النادرة الاستخدام إلى استيراد ديناميكي بدل رفع حد التحذير فقط.

يحمل `client/index.html` الأساسيات العربية والاتجاه من اليمين لليسار والعنوان، لكنه يحتاج وصفًا فريدًا للمنصة ووسوم Open Graph وTwitter وcanonical و`robots.txt` وسياسة فهرسة واضحة قبل الاعتماد على الظهور في البحث. لا يُنصح بادعاء جاهزية SEO كاملة قبل إضافة هذه البيانات ومراجعة النص الذي يظهر للمشاركة.

| المجال | الوضع الحالي | التحسين المقترح |
|---|---|---|
| التحميل الأول | تحذير حزمة رئيسية فوق 500KB | تحليل الحزم وتحميل مكتبات العرض/الرسوم عند الحاجة. |
| الأخطاء | توجد اختبارات خادم منضبطة ورسائل عربية في أغلب الحواجز | إضافة مراقبة أخطاء تشغيلية مجهولة الهوية قبل زيادة المستخدمين. |
| قابلية الصيانة | ملفات واجهة كبيرة نسبيًا مثل `Portal.tsx` | فصل نماذج الإدارة إلى مكونات أصغر مع اختبارات سلوك، وليس إعادة كتابة. |
| SEO | أساس HTML جيد، بيانات مشاركة ناقصة | إضافة meta description وOG/canonical وrobots ومراجعة صفحات عامة قابلة للفهرسة. |

## الضوابط التي وُجدت وتعمل

العمليات المحمية تمنع الحساب غير النشط، وطبقة الأدوار تميز المدير والمعلّم والطالب. يتحقق المعلّم من ملكية مجموعته، والطالب من عضويته أو ملكية محاولته. يحمي React من إدراج HTML النصي المعتاد، ولم يعثر البحث في شفرة العميل على واجهات إدخال HTML خام أو تنفيذ شيفرة ديناميكية. كما أن حقول روابط Zoom وTelegram المدخلة حديثًا تمر عبر تحقق URL في مخطط الخادم.

هذه ضوابط جيدة لكنها لا تغني عن تدقيق تلقائي دوري للحزم، ومعاملات البيانات، وتحقق الجلسة/CSRF المذكورة أعلاه.

## التحقق المنفذ وخطة التحقق اليدوي

تم تشغيل `pnpm check` و`pnpm test` و`pnpm build` بنجاح؛ حصلت 35 حالة اختبار على النجاح. كما فُحصت الرؤوس محليًا وتأكدت CSP ورفض الإطارات وسياسات الإحالة والميزات، مع اختفاء `x-powered-by`. فُحصت قاعدة البيانات لرسائل البريد المكررة قبل تطبيق القيد ولم تظهر سجلات مكررة.

| البند | الإجراء اليدوي المطلوب | الحالة |
|---|---|---|
| تسجيل الدخول المنشور | تجربة OAuth في النطاق المنشور، والعودة للمنصة، وتسجيل الخروج ثم الدخول مجددًا. | **يتطلب تحققًا يدويًا**. |
| الصورة الشخصية | رفع JPEG/PNG/WebP صالح يقارب الحد، ثم التأكد من عرضها واستمرارها بعد التحديث. | **يتطلب تحققًا يدويًا** بحساب طالب أو معلّم فعلي. |
| المعاينة ضمن الإطار | فتح المعاينة داخل Manus بعد النشر؛ سياسة CSP تسمح لـ `manus.im` ونطاقاته الفرعية فقط. | **تم التحقق من السياسة محليًا؛ يلزم تحديث المتصفح**. |
| مساحات الأدوار | تجربة تبويبات المعلّم ومساحة مادة الطالب ببيانات فعلية بعد آخر تبسيط للواجهة. | **يتطلب تحققًا يدويًا**. |
| النشر | فحص الرؤوس وتسجيل الدخول في النطاق المنشور بعد حفظ الإصدار الجديد. | **ينفذ بعد النشر**. |

## الأولويات المقترحة للدورة التالية

ابدأ بحماية الجلسة وCSRF بعد اختبار OAuth في بيئة منشورة، ثم عالج المعاملات الذرية لعمليات الحضور والاختبار، وبعدها حدّث تبعيات Express/عرض المخططات في دورة توافق منفصلة. بالتوازي، أضف وصف SEO وبيانات المشاركة، ثم حلل الحزمة الرئيسية قبل أي تحسين شكلي واسع.
